.DS_Store

# `dist` folder with the export of SSG
dist/

# Local Netlify folder
.netlify

# Generated by Cargo
# will have compiled files and executables
debug/
target/

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Image generator
generated_images/

node_modules/
package-lock.json
.aider*

# Tailwind output — build artifact, regenerated by `just tailwind_build` /
# `just dev` / CI (deploy + preview build it fresh; tests never read it)
styles/output.css
